pipeline {
     agent any
     stages {

     stage ('Kafka')
     {
        steps {
            build job: 'TestPipeline', parameters: [string(name:'Environment', value: "production")]
            bat "producer2.py"
        }
    }
 }
}

